# MaryOS: iwd finds and joins Wi-Fi networks (System Settings › Network runs
# iwctl); systemd-networkd gives the link its address, as it does the wired
# one (etc/systemd/network/25-wireless.network).
[General]
EnableNetworkConfiguration=false

[Network]
NameResolvingService=systemd
