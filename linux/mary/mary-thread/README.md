# mary-thread — Swift twin: `Mary/Sources/MaryThread`

Mary's client for the local Thread node: `mt_deposit_turn` (ThreadQuery.Index), `mt_library` and
`mt_documents`, the calls `ThreadDirectClient.swift` makes.

Status: planned (commit 14). Prefix `mt_`.
