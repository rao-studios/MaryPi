# brain — Swift twin: `Mary/Sources/MaryBrain`

The converse turn: the voice-lane instructions (`Prompt/PromptCatalog+Voice.swift`), Mary's persona,
a history of at most twelve spoken messages, and the request `sewnd` runs. Routing, Lane B and skill
dispatch come later; `mb_tools` is declared for the milestone that lets the conversation call skills.

Status: planned (commit 10). Prefix `mb_`.
